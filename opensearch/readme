# Generate new certs
openssl req -x509 -newkey rsa:4096 -sha256 -days 3650 \
  -nodes \
  -keyout certs/opensearch.key \
  -out certs/opensearch.crt \
  -subj "/CN=opensearch" \
  -addext "subjectAltName=DNS:opensearch,DNS:localhost"